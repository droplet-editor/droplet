Block (linked list of Lines)
  Line (can start with a BlockField, linked list of Fields, can end with a BlockField)
    Field (can point to a Block or a String)
      *Block
    BlockField
      *Line before
      *Block
      *Line after
